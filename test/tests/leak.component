<can-component tag="leak-scope" leak-scope>
	<view>
		<content/>
	</view>
	<script type="view-model">
		var DefineMap = require("can-define/map/map");

		module.exports = DefineMap.extend({
			foo: { default: "bar" }
		});
	</script>
</can-component>
